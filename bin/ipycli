#!/usr/bin/env python
from ipycli.notebookapp import NotebookApp

if __name__ == '__main__':
    import sys

    argv = sys.argv[1:] 

    app = NotebookApp.instance()
    app.initialize(argv)
    app.start()
